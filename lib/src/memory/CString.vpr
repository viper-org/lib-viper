namespace std {
    func @memcpy(p1: i8*, p2: i8*, count: i32) -> i8* {
        while (count > 0) {
            *p1 = *p2;
            count -= 1;
            p1 = p1 + 1;
            p2 = p2 + 1;
        }

        return p1;
    }
}